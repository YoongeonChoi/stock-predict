# Backend API base URL.
# Leave blank in an integrated same-origin deployment.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Supabase browser auth config.
# Leave blank if you only inspect public routes without login flows.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
