"""Internal market-service helper modules.

Thin package used by ``market_service.py`` so the public import surface stays stable
while quick/full/fallback helpers live in smaller focused modules.
"""

