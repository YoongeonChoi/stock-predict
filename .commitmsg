feat: add structured error code system (SP-XYYY) across entire stack

- Create centralized error registry (backend/app/errors.py) with 30+ codes
  across 6 categories: Config, Data, Analysis, LLM, Service, Validation
- Apply error codes to all 8 routers with JSONResponse + error_code field
- Apply error codes to all analyzers, data clients, and LLM client
- Add global exception handler (SP-9999) in main.py
- Create frontend ErrorBanner + WarningBanner components with Korean guides
- Create ApiError class in api.ts to parse server error codes
- Apply error banners to Country, Sector, Stock pages
- Update README with full error code reference table and resolution steps
- Rewrite README setup guide: clarify one-time vs every-run steps
